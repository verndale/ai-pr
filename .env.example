# Copy to .env. Optional vars (PR_BASE_BRANCH, PR_DRAFT, PR_AI, …) are documented in README.
GH_TOKEN=

# @verndale/ai-commit (maintainers: pnpm commit / git hooks only — not part of the published ai-pr package)
# OPENAI_API_KEY=
# COMMIT_AI_MODEL=
